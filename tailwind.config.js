/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Scans your files for Tailwind classes
  ],
  theme: {
    extend: {}, // Extend Tailwind's default theme if needed
  },
  plugins: [],
};
